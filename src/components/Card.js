import React from 'react'

const Card = ({ title }) => {
    return (
        <div style={{ margin: '10px 10px', backgroundColor: 'teal' }}>
            <h1>{title}</h1>
        </div>
    )
}

export default Card
