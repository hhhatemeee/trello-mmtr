import React from 'react'

const ElementList = ({ text }) => {
    // console.log(boardId);
    // let elements = []
    // console.log(elements);
    return (

        <div>
            <h5>{text}</h5>
        </div>
    )
}

export default ElementList
